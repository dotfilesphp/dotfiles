services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true
    bashit.install:
        class: Dotfiles\Plugins\BashIt\Listeners\InstallSubscriber
        calls:
            - [setInstallDir, ['%dotfiles.install_dir%']]
        tags:
            - { name: "event_subscribers" }
