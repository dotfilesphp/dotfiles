services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true
    Dotfiles\Plugins\Bash\Listeners\InstallListener:
        calls:
            - [setInstallDir, ['%dotfiles.install_dir%']]
        tags:
            - { name: "event_subscribers" }

    #bash.listeners.install:
    #    class: Dotfiles\Plugins\Bash\Listeners
    #    #event_name: 'dotfiles.install'

