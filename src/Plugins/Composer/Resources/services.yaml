services:
    _defaults:
        autoconfigure: true
        autowire: true
        public: true
    composer.listeners.install:
        class: Dotfiles\Plugins\Composer\Listeners\InstallSubscriber
        tags: { name: event_subscribers }
