services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true
    phpcs.listeners.install:
        class: Dotfiles\Plugins\PHPCSFixer\Listeners\InstallSubscriber
        tags: { name: "event_subscriber" }
