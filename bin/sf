#!/usr/bin/env bash
source ../lib/base.sh

CMD="app/console"
if [ -e $CMD ]; then
    CMD="bin/console"
fi

if [ ! -e $CMD ]; then
    mError "No executable symfony command in this directory"
else
    $CMD $*
fi
