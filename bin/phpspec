#!/usr/bin/env bash

source ../lib/base.sh

CMD="vendor/bin/phpspec"
if [ ! -e $CMD ]; then
    CMD="bin/phpspec"
fi

if [ ! -e $CMD ]; then
    mError "No executable phpspec found in this directory"
else
    $CMD $*
fi
